{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cartographer/atlas/schemas/console.schema.json#1",
  "title": "ConsoleRecord",
  "description": "Browser console message record for Atlas v1.0 - Owner: Cai Frazier. Full mode only.",
  "type": "object",
  "required": ["pageUrl", "type", "message", "source", "timestamp"],
  "additionalProperties": false,
  "properties": {
    "pageUrl": {
      "type": "string",
      "format": "uri",
      "description": "The page that logged the message"
    },
    "type": {
      "type": "string",
      "enum": ["log", "warn", "error", "info"],
      "description": "Console message type"
    },
    "message": {
      "type": "string",
      "description": "The log message or formatted string"
    },
    "stackTrace": {
      "type": "string",
      "description": "Stack trace if error"
    },
    "source": {
      "type": "string",
      "enum": ["page", "browser"],
      "description": "Source of the message (only 'page' is stored)"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the message was logged"
    }
  }
}
